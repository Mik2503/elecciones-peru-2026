# Docker synchronization engine for Peru 2026 Elections
# This ensures 1-minute updates without using paid Vercel Crons.

FROM alpine:latest

# Install curl
RUN apk add --no-cache curl

# Configuration
COPY ./scripts/sync_official.sh /sync.sh
RUN chmod +x /sync.sh

# Sync Loop
CMD ["/sync.sh"]
